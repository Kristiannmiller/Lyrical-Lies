import { render, screen } from '@testing-library/react';
import App from './App';

test('works', () => {
  expect(true).toBe(true);
});


// TESTING IDEAS ::

// It should render a header
  //expect that alt text is in document for each
